LET split =
    REC split' sez ->
        MATCH sez WITH
        | [] -> {[], []}
        | e :: es -> 
            LET repa = split' es
            IN
            {(FST e) :: (FST repa), (SND e) :: (SND repa)}
IN
split ({1 + 1, 2} :: {3, 4} :: ((FUN x -> {2 * x, x * x}) 12) :: {7, 21} :: [])
